services:
  - type: web
    name: whystock
    runtime: python
    plan: free
    buildCommand: ""
    startCommand: python server.py --host 0.0.0.0 --port $PORT
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.4
